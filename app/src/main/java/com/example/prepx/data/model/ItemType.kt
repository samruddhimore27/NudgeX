package com.example.prepx.data.model

/**
 * Categorization enum for planner entries.
 * Dynamic styling (chip color, stroke, badge text) is derived from this type.
 */
enum class ItemType {
    CONTEST,
    CLASS,
    EXAM,
    TASK,
    GOAL
}
